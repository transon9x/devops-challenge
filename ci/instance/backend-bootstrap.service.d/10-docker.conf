# Installed by the container-fleet AMI only. A bundle fleet has no Docker, and the
# bootstrap calls the Docker CLI before starting backend.service, so the dependency has to
# be on the bootstrap unit rather than on the service it starts.
[Unit]
Requires=docker.service
After=docker.service
